/**
 * Valores comuns ao aplicativo.
 */
const COMMON = {
    DEFAULT_DATE_FORMAT: "yyyy-mm-dd",
    PASSWORD_MAXLENGTH: 20,
    IMAGE_MIN_FILE_SIZE: 50, // 1MB
    IMAGE_MAX_FILE_SIZE: 5120, // 5MB
}

export default COMMON;